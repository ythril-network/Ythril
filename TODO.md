# TODO

Items marked ✅ were completed in v0.5.0–v0.7.0.

## Completed

- ✅ Space collision on network join → resolved with spaceMap aliasing + merge/alias UI (v0.7.0)
- ✅ Show which networks are connected to each space → network badges on space rows
- ✅ Remove additional top navigation in settings → sidebar-only navigation
- ✅ Checkbox space selectors in networks/tokens → replaced comma-separated text inputs
- ✅ Remove "This brain's URL" UI section → field kept in background
- ✅ Rename spaces description to "MCP Description"
- ✅ Prefill spaces description with generic endpoint descriptions
- ✅ Remove titles on focus pages (Brain, etc.)
- ✅ Side-by-side layout for Create/Join Network on wide screens